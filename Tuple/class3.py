#Tuple

student=("Alice", 20, "Computer Science")
print(student)
print(student[0])  # Accessing the first element
print(student[1])  # Accessing the second element

#Tuples are immutable, so you cannot change their elements
# student[1] = 21  # This will raise a TypeError

